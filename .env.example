# Optional. If set, server uses PostgreSQL for persistent shared data.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME

# Optional. Used only when DATABASE_URL is not set.
DATA_DIR=./data

# Optional. Set true for one deployment to import data/store.json into Postgres when DB is empty.
BOOTSTRAP_FROM_FILE=false

# Optional.
PORT=3000
